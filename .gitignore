# Python
.venv/
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/

# Jupyter
.ipynb_checkpoints/
*.ipynb_checkpoints

# Binary model files — besar & binary, tracked di HF Hub
# JSON/CSV/PNG di artifacts/ tetap di-commit (dokumentasi portfolio)
*.pkl
*.joblib

# MLflow local tracking (64MB, regenerable dari training script)
mlruns/

# Raw HuggingFace cache
banking77_raw/
~/.cache/huggingface/

# Nested repo — HF Space terpisah, isinya sudah ada di serving/
banking-intent-router/

# Branding & marketing (bukan bagian dari kode)
branding/

# Env & secrets
.env
*.env
.env.*
!.env.example

# Monitoring runtime data
grafana-data/

# Evaluation runtime output — dataset/generated_eval_set.json tetap di-commit
# (contoh hasil run, dokumentasi portfolio); ini cache/output run lokal saja
.deepeval/
.deepeval_cache.json
evaluation/ragas_eval/ragas_results.csv

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
