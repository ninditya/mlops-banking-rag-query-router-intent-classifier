# Serving only — CPU-only torch (jauh lebih kecil dari CUDA version)
scikit-learn==1.5.1
numpy==1.26.4
pandas==2.2.2
sentence-transformers==5.5.0
transformers==5.8.1
tokenizers==0.22.2
huggingface_hub==1.15.0
faiss-cpu==1.13.2               # default backend (RAG_BACKEND=faiss, in-process)
# qdrant-client==1.19.0         # opsional -- pasang manual kalau pakai RAG_BACKEND=qdrant
fastapi==0.136.1
uvicorn[standard]==0.47.0
pydantic==2.13.4
starlette==1.0.0
httpx==0.28.1
python-dotenv==1.2.2
prometheus-client==0.21.1
cramjam==2.11.0

# torch CPU-only — install terpisah via PyTorch index
# (dipasang di Dockerfile langsung, bukan lewat file ini)
